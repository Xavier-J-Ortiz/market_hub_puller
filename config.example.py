# When config is ready, rename to config.py to allow the script to use information
region_hubs = {
        "RegionName1": ["RegionId1", "StationId1", "ToonId1"], # Jita should be this first entry
        "RegionName2": ["RegionId2", "StationId2", "ToonId2"],
        }
